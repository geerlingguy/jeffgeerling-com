---
version: "2"
options:
  compose-file-path: 'docker-compose.yml'
  verbose: false
  max_attempt: 5
  project_root: 'pwd'

syncs:
  docker-sync:
    sync_strategy: 'native_osx'
    src: './'
    host_disk_mount_mode: 'cached'
